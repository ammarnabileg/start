# Hatchy Admin Panel (PHP native + Tailwind)

لوحة تحكم كاملة للمنصّة (Super Admin) تتصل بنفس قاعدة بيانات Supabase الخاصة
بتطبيقَي العميل والتاجر، وتعطيك **تحكّمًا كاملًا** (عرض/إضافة/تعديل/حذف) مع
**حسابات مسؤولين وأدوار وصلاحيات** كاملة.

## المميزات
- 🔐 **مصادقة مسؤولين** (bcrypt) + جلسات + حماية CSRF على كل النماذج.
- 🛡️ **أدوار وصلاحيات** لكل مورد × إجراء: `عرض / إضافة / تعديل / حذف / اعتماد`
  (Super Admin / Manager / Viewer جاهزة + إنشاء أدوار مخصّصة بمصفوفة صلاحيات).
- 📊 **لوحة تحكم**: مستخدمون جدد لفترة (٧/٣٠/٩٠/سنة)، تجار حسب الحالة،
  اشتراكات فعّالة، بلاغات مفتوحة، نقاط، زيارات، مخطّط تسجيلات.
- 🏪 **CRM التجار**: بحث/تصفية + اعتماد/إيقاف/رفض/إعادة تفعيل + حذف + تعديل
  بيانات + إدارة الاشتراك + عرض الفروع/الموظفين/العملاء (اعتماد ينشئ تجربة تلقائيًا).
- 👥 **المستخدمون**: بحث/تصفية + عرض/تعديل/حذف + إحصاءات لكل مستخدم +
  **إضافة متعدّد لقائمة/أكثر بضغطة**.
- ◑ **القوائم/الشرائح**: إنشاء/حذف + عدّاد الأعضاء + إرسال إشعار للقائمة.
- 🔔 **إشعارات**: إرسال لكل المستخدمين أو لقائمة محدّدة (يُكتب فورًا داخل التطبيق).
- ⚑ **الشكاوى والبلاغات**: تصفية بالحالة + تغيير الحالة (مفتوح/مراجعة/محلول) + حذف.
- 🗒 **سجلّ تدقيق** لكل إجراء حسّاس.

## البنية والأمان
- PHP native (لا أُطُر) + Tailwind (CDN) + RTL + خط Tajawal — نفس هوية Hatchy.
- اتصال مباشر بـ Postgres عبر **PDO باستعلامات مُحضّرة دائمًا** (لا حقن SQL).
- جداول اللوحة في **سكيمة `admin` منفصلة** — **يجب إبقاؤها خارج «Exposed schemas»**
  في إعدادات Supabase API حتى لا يصل إليها anon/authenticated إطلاقًا.
- اللوحة تتصل بصلاحية كاملة (تتجاوز RLS) لتعطيك تحكّمًا كاملًا — احمِ `config.php`
  وشغّل اللوحة خلف شبكة/IP موثوق + HTTPS.

## أدوات متقدّمة (v2)
- ⭐ **منح/خصم نقاط يدوي** لمستخدم أو قائمة — ذرّي عبر `wallet_apply` + إعادة حساب
  المستوى + تسجيل `adjust` بالسبب (دالة `admin.bulk_adjust_points`).
- ◑ **شرائح ذكية (Smart Lists)** — معايير (نقاط/نشاط/تاريخ انضمام/مشاركة) تتحدّث
  تلقائيًا (دالة `admin.smart_list_users`).
- ⬇️ **تصدير CSV** (UTF‑8 BOM لإكسل العربي) للمستخدمين/التجار/البلاغات.
- 🔔 **Push فعلي** — دالة `supabase/functions/admin-push` ترسل FCM لقائمة/الكل عبر
  `sendPush` المشتركة (الإشعار داخل التطبيق يُكتب دائمًا).
- 💳 **المالية**: MRR/ARR متوقّع، تجارب على وشك الانتهاء، تجديدات قادمة، أسعار باقات.
- 📈 **تحليلات**: الاحتفاظ، أكثر المتاجر عملاءً، توزيع المستويات، النقاط يوميًا.
- 🚫 **حظر + أجهزة**: حظر مستخدم (يزيل توكناته فورًا) + إدارة `device_tokens`.
- 🔐 **2FA (TOTP)** لحسابات المسؤولين + **قفل بعد ٥ محاولات** + **سجلّ دخول**.
- 📝 **مركز محتوى**: تعديل `platform_settings` (شروط/خصوصية/دعم/أدنى إصدار/صيانة).

## أدوات إضافية (v3)
- 👁 **معاينة الشريحة الذكية** (`list_view.php`) + **حفظ كلقطة ثابتة** (تجميد الأعضاء).
- ⬆️ **استيراد CSV للقوائم**: ألصق/ارفع أرقامًا أو إيميلات → مطابقة وإضافة دفعة.
- ⬇️ **تصدير Excel حقيقي** (XLSX/OOXML بدون مكتبات) بجانب CSV.
- 🖨️ **تقرير PDF** للطباعة/الحفظ (RTL صحيح عبر المتصفّح) — `report.php`.
- ⏰ **تقرير مجدول بالبريد** (XLSX مرفق) — `cron_report.php` + `report_recipients`.
- 💰 **استرجاع الإيرادات (Dunning)**: متابعة `past_due/expired` + تذكيرات يدوية/تلقائية
  (`dunning.php` + `cron_dunning.php`، بلا تكرار خلال ٧ أيام).
- ❤ **صحة النظام** (`health.php`): أحجام الجداول، أجهزة Push، مهام cron وآخر تشغيل،
  أخطاء جهة اللوحة (أخطاء دوال الحافة تبقى في Supabase Logs).

### جدولة (cron)
```cron
0 7 * * 1  php /path/admin-web/cron_report.php     # تقرير أسبوعي بالبريد
0 9 * * *  php /path/admin-web/cron_dunning.php    # تذكيرات تجديد يومية
```
> تصدير Excel يتطلّب امتداد `zip` · تقرير البريد يتطلّب MTA (sendmail/SMTP).
> تقرير PDF بالعربية يُحفظ من المتصفّح؛ لـ PDF مرفق بالبريد استخدم مكتبة مثل mPDF.

## التثبيت
1. **طبّق سكيمات الأدمن** على قاعدة البيانات:
   ```bash
   psql "$SUPABASE_DB_URL" -f sql/admin_schema.sql
   psql "$SUPABASE_DB_URL" -f sql/admin_schema_v2.sql
   psql "$SUPABASE_DB_URL" -f sql/admin_schema_v3.sql
   ```
   ولتفعيل Push: انشر دالة `admin-push` واضبط `ADMIN_PUSH_SECRET` و`FCM_SERVICE_ACCOUNT`
   في Supabase، ثم احفظ رابط الدالة والسر من صفحة «الإشعارات».
2. **الإعداد**: `cp config.sample.php config.php` ثم اضبط `db_dsn/db_user/db_pass`
   (سلسلة اتصال Postgres المباشرة من Supabase: Settings → Database).
3. **شغّل**: على خادم PHP (Apache/Nginx + PHP-FPM) أو للتجربة:
   ```bash
   php -S 0.0.0.0:8080
   ```
4. افتح `install.php` لإنشاء أول حساب **Super Admin** (مرة واحدة) ثم `login.php`.

> متطلبات: PHP 8.1+ مع `pdo_pgsql`.

## الصلاحيات
كل دور = خريطة JSON: `{"merchants":["view","edit","approve"], ...}`. الـ Super
Admin يملك كل شيء. `admins` و`roles` مقصورتان على Super Admin. كل زر/إجراء في
الواجهة مُقيَّد بـ `can($resource,$action)` ومُتحقَّق منه ثانيةً في الخادم.

## ملفات
```
admin-web/
  config.sample.php        نموذج الإعداد (انسخه إلى config.php)
  sql/admin_schema.sql     سكيمة admin (roles/users/lists/members/audit)
  lib/                     db, helpers, auth, rbac, audit, boot
  partials/                header (Tailwind shell + sidebar), footer
  install.php login.php logout.php
  index.php                لوحة التحكم + الإحصاءات
  merchants.php merchant_view.php   CRM التجار + الموافقات
  users.php user_view.php  المستخدمون + إضافة للقوائم
  lists.php                القوائم/الشرائح
  notifications.php        إرسال الإشعارات
  reports.php              الشكاوى والبلاغات
  admins.php roles.php     حسابات المسؤولين + الأدوار
  audit.php                سجلّ التدقيق
```
